// NodeSyncService has been removed in Phase 18.
// Sync orchestration is now handled by the backend SyncPullService.
// The desktop app uses POST /api/sync/preview and POST /api/sync/pull on the local backend.
