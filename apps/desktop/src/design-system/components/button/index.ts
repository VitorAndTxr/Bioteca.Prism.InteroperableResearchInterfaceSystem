/**
 * Button Component - Barrel Export
 *
 * Exports the Button component and related types for use throughout the application.
 *
 * @example
 * ```tsx
 * import { Button } from '@/design-system/components/button';
 * import type { ButtonProps, ButtonVariant } from '@/design-system/components/button';
 * ```
 */

export { default as Button } from './Button';
export type { ButtonProps, ButtonVariant, ButtonSize, ButtonIconPosition } from './Button.types';
